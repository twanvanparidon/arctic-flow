# Generated from a build that produced a working binary:
#   docker run --rm atf-build pip freeze | grep -viE '^(pip|setuptools|wheel)=='
#
# Pinned because a frozen binary embeds these wheels. The build environment is part
# of the artefact, so two builds of the same commit must not quietly differ. Regenerate
# after changing pyproject.toml's dependencies; verify_deps.py fails the build if this
# file and pyproject.toml disagree.
altgraph==0.17.5
attrs==26.1.0
cffi==2.1.1
cryptography==50.0.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
packaging==26.3
pycparser==3.0
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
PyYAML==6.0.3
referencing==0.37.0
rpds-py==2026.6.3
