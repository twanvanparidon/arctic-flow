# 1.4.0

- push-based flows: a step declares where it hands off, not what it waits for
- vault: per-step secret grants, injected as environment variables
- diagram: a static resolution report alongside the Mermaid graph
